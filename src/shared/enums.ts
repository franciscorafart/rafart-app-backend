export enum Roles {
  Fan = 20,
  SuperFan = 30,
  Admin = 40,
  Superadmin = 50,
}

export enum ResponseStatus {
  Ok = "ok",
  BadRequest = "bad_request",
  Unauthorized = "unauthorized",
  Error = "server_error",
}
